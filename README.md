## IPTV Player

Flutter IPTV player for Android with:

- M3U playlist URL loading
- pasted M3U playlist loading
- channel search, groups, and favorites
- native Android video playback through `VideoView`

Run it with:

```sh
flutter run -d <device>
```
